import { createTheme } from "@mui/material";

export const tableTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#3f51b5",
    },
    secondary: {
      main: "#d57cd8",
    },
  },
});
